name: Publish to npm + MCP Registry

# Push a version tag (e.g. v0.1.0) to publish the npm package and register the
# server in the Official MCP Registry. The MCP Registry uses GitHub OIDC — no
# token to manage there. npm publish needs an NPM_TOKEN repo secret.

on:
  push:
    tags: ["v*"]
  workflow_dispatch: {}

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # OIDC for the MCP Registry
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "lts/*"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test

      - name: Publish package to npm
        # Tolerant: if this exact version is already on npm (e.g. published
        # out-of-band after an npm rate-limit), don't fail the run — let the
        # MCP Registry step proceed. Only a genuine publish error fails.
        run: |
          set +e
          out=$(npm publish --access public 2>&1)
          echo "$out"
          code=$?
          if [ $code -ne 0 ]; then
            if echo "$out" | grep -qiE "cannot publish over|previously published|EPUBLISHCONFLICT|403 .*already"; then
              echo "Version already on npm — continuing."
              exit 0
            fi
            exit $code
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Install mcp-publisher
        run: |
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/').tar.gz" | tar xz mcp-publisher

      - name: Authenticate to MCP Registry (OIDC)
        run: ./mcp-publisher login github-oidc

      - name: Publish server to MCP Registry
        run: ./mcp-publisher publish
